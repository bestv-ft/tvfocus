<template>
    <focus class="{{$global.nav}}" id="nav" name="category"></focus>
    <focus class="{{$global.content}}" name="content" scroll="x"></focus>
</template>
<script>
import Style from '../style/main.css';
import category from './nav.focus';
import content from './content.focus';
export default {
    created (e) {
        e.data.$global = Style;
    }
}
</script>